# Angular Template
## To install
- Clone the repository to your local system.
- Run `npm install` (You might have [Node JS](https://nodejs.org/) installed)
- Run `bower install` (You might have Bower installed - `npm install -g bower`)

## To launch
- Run `gulp` or `gulp serve`
